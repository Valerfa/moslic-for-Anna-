import React, {
  useEffect,
  useRef,
  useState,
  useMemo,
} from "react";
import axios from "axios";


const Main = ({token, personlog_id, resources}) => {

  const [loading, setLoad] = useState(false);
  const [error, setError] = useState(null);

  return (
    <>
    </>
  );
};

export default Main;
